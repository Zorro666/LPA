Short
* LPA divide use a power of 2 for divisor - need shift functions (left & right)
* Run valgrind and fix up memory leaks and uninit memory

Medium
* LPA divide - perhaps need a function for the multiply & subtract
* Optimise on LPA divide - stop the digit copying, only need 3 digits worth

Long
* Try out Karatsuba algorithm for multiply
* BCD : Normalize input sizes to remove if length tests (and BCD do the unpack of nibbles to bytes)
* BCD : Unpack BCD nibble packed numbers into single byte arrays to make functions simpler
* BCD : switch to unpacked storage much faster
* Work out what to optimise on BCD divide - it is horrible right now
* BCD divide - perhaps need a function for that multiply & subtract
* Work out what other math functions are needed

Blue Sky
* Implement BCD pow function
* Test BCD pow function
* Implement BCD exp function
* Test BCD exp function
* Implement LPA pow function
* Test LPA pow function
* Implement LPA exp function
* Test LPA exp function

